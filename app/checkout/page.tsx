import { CheckoutForm } from '@/components/checkout-form';

export const metadata = {
  title: 'Checkout | Luminara Stories',
  description:
    'Complete your order and prepare to receive hand-curated storybooks delivered with care.'
};

export default function CheckoutPage() {
  return <CheckoutForm />;
}
